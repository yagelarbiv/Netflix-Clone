import { Sort } from './sort.js';
export function extractSort(sort) {
    if (sort instanceof Array) {
        let r = new Sort();
        sort.forEach((i) => {
            r.Segments.push(i);
        });
        return r;
    }
    return sort;
}
